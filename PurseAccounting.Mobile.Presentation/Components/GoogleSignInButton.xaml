<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PurseAccountinng.Mobile.Presentation.Components.GoogleSignInButton">
    <Border 
        x:Name="GoogleButtonBorder"
        BackgroundColor="{StaticResource WorkBackground}"
        Stroke="{StaticResource InactiveElementFill}"
        StrokeThickness="1"
        HeightRequest="{StaticResource ButtonHeight}"
        Padding="0">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="{StaticResource CornerRadiusLarge}" />
        </Border.StrokeShape>
        <Grid>
            <Image
                Source="google.png"
                WidthRequest="24"
                HeightRequest="24"
                VerticalOptions="Center"
                HorizontalOptions="Start"
                Margin="16,0,0,0" />
            <Label
                Text="Google"
                TextColor="{StaticResource Gray1}"
                FontSize="{StaticResource FontSizeRegular}"
                FontFamily="OpenSansSemibold"
                VerticalOptions="Center"
                HorizontalOptions="Center" />
        </Grid>
    </Border>
</ContentView>